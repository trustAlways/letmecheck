<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    >
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <GridView

        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:id="@+id/grid"
        android:layout_margin="@dimen/mrg2"

        />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/grid"

        >


    <Button
        android:id="@+id/btn_loadmore"
        android:layout_width="@dimen/size6"
        android:layout_height="@dimen/size3"
        android:layout_centerHorizontal="true"
        android:text="@string/load_more"
        android:textColor="@color/white"
        android:textAllCaps="false"

        android:background="@drawable/loadmorebuitton_icon"
        />

        <ImageView
            android:id="@+id/img_arrow_down"
            android:layout_width="@dimen/mrg5"
            android:layout_height="@dimen/size0"
            android:layout_below="@+id/btn_loadmore"
            android:layout_centerHorizontal="true"
            android:background="@drawable/double_arrow_down_icon"
            />


    </RelativeLayout>
</RelativeLayout>
    </ScrollView>

</RelativeLayout>